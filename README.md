# AI-Resume-Portfolio-Optimizer
Monorepo that analyzes resumes and portfolios using backend AI agents (extraction, gap analysis, rewrite, suggestions, validation) and a Vite + React frontend analyzer. Includes API, DB utilities, and tooling for parsing and normalizing resume data.
